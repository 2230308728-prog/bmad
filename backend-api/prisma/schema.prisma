// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/dprisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

// 用户角色枚举
enum Role {
  PARENT // 家长用户
  ADMIN // 管理员
}

// 用户状态枚举
enum UserStatus {
  ACTIVE // 激活状态
  INACTIVE // 未激活
  BANNED // 已禁用
}

// 用户模型
// 设计说明：openid 可为 NULL 是有意为之
// - PARENT 用户：必须有 openid（微信授权登录）
// - ADMIN 用户：openid 为 NULL（使用账号密码登录）
// - @unique 约束在 PostgreSQL 中允许多个 NULL 值，这符合我们的业务需求
model User {
  id        Int        @id @default(autoincrement())
  openid    String?    @unique // 微信 OpenID，唯一索引（允许多个 NULL）
  nickname  String? // 用户昵称
  avatarUrl String?    @map("avatar_url") // 头像 URL
  phone     String? // 手机号（加密存储）
  role      Role       @default(PARENT) // 用户角色
  status    UserStatus @default(ACTIVE) // 用户状态
  createdAt DateTime   @default(now()) @map("created_at")
  updatedAt DateTime   @updatedAt @map("updated_at")

  @@map("users") // 表名映射为小写复数
}
